#include <bits/stdc++.h>

using namespace std;

int main() {
  string s;
  getline(cin,s);

  if (s.compare("Hello World") == 0) {
    printf("OK");
  } else {
    printf("Wrong Answer");
  }
  return 0;
}
